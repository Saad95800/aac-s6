{% extends 'layout_admin.html.twig' %}

{% block body %}
    <div class="container">
        <h2 class="text-center">Menu {{ type|capitalize }}</h2>
        
        <div class="row" style="margin-top: 30px;">
            <div class="col-sm-4 text-center">
                <a href="/admin/publication/liste?type={{type}}">
                    <button class="btn btn-primary">Liste</button>
                </a>
            </div>
            <div class="col-sm-4 text-center">
                <a href="/admin/publication/add?type={{type}}">
                    <button class="btn btn-primary">Ajouter</button>
                </a>
            </div>
            <div class="col-sm-4 text-center">
                <a href="/admin/publication/delete?type={{type}}">
                    <button class="btn btn-primary">Supprimer</button>
                </a>
            </div>
        </div>
    </div>
{% endblock %}