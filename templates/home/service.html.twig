{% extends 'layout.html.twig' %}

{% block body %}

<div class="container">
  <h1 class="text-titles text-center">{{service.name}}</h1>
  <p class="text-center"><img src="{{ asset(service.image.url) }}" style="max-width: 200px"/></p>
  <p>{{service.description}}</p>
</div>
{% endblock %}

